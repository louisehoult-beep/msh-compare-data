(function(){
var D={"vascular":{"label":"Vascular access","route":[{"name":"NHS Supply Chain — Intravenous Cannula and Associated Products","dates":"27/03/2023 – 31/03/2027 (incl. extension)","url":"https://www.supplychain.nhs.uk/product-information/contract-launch-brief/intravenous-cannula-and-associated-products/"},{"name":"NHS Supply Chain — Central Venous Catheters and Associated Products","dates":"23/03/2026 – 22/03/2028","url":"https://www.supplychain.nhs.uk/product-information/contract-launch-brief/central-venous-catheters-and-associated-products/"}],"routeNote":"Covers PICCs, CVCs, securement (incl. SecurAcath) and tip confirmation under the CVC framework; peripheral cannulae under the IV Cannula framework.","types":{"pivc":"Peripheral IV cannula","picc":"PICC","mid":"Midline","sec":"Securement & dressings"},"suppliers":[{"co":"Becton Dickinson (BD)","brands":"Nexiva · PowerPICC · Venflon","t":["pivc","picc"],"url":"https://www.bd.com/en-us/products-and-solutions/products/product-families/bd-nexiva-catheter-system","note":"On both frameworks. Venflon range delisting completes 31/07/2026.","iss":[1,2]},{"co":"B. Braun Medical","brands":"Introcan Safety · Introcan Safety 3","t":["pivc"],"url":"https://www.bbraun.co.uk/en/products-and-solutions/therapies/infusion-therapy/introcan-safety--3.html","note":"Awarded on both frameworks.","iss":[]},{"co":"Teleflex Medical UK","brands":"Arrow PICC · Arrowg+ard Blue Advance","t":["picc"],"url":"https://www.teleflex.com/usa/en/product-areas/vascular-access/central-access/peripherally-inserted-cc/index.html","note":"On the CVC framework 2026–28.","iss":[]},{"co":"Vygon (UK)","brands":"Lifecath PICC · Lifecath Midline · Smartmidline · Bionector","t":["pivc","picc","mid"],"url":"https://vygon.co.uk/category/vascular-access/","note":"On both frameworks.","iss":[]},{"co":"ICU Medical (incl. Smiths Medical)","brands":"Jelco · Jelco Seriva","t":["pivc"],"url":"https://www.smiths-medical.com/en-us/brands/jelco","note":"Multiple entities listed across both frameworks.","iss":[]},{"co":"GBUK Group","brands":"Securasite · Grip-Lok · Nouvo Safety Set · Polywin PolySafety","t":["pivc","sec"],"url":"https://gbukgroup.com/products/vascular/","note":"GBUK Ltd on IV Cannula framework; GBUK Healthcare on CVC framework. CVC/PICC securement and fixation range.","iss":[]},{"co":"Terumo UK","brands":"Surflo · Surshield","t":["pivc"],"url":"https://www.terumo-europe.com/en-emea/products/surflo%E2%84%A2-i-v-catheter","note":"Two listings on the IV Cannula framework.","iss":[]},{"co":"Kimal PLC","brands":"SV BioFlo PICC · SV BioFlo Midline","t":["picc","mid"],"url":"https://kimal.com/en/product/bioflo-picc/","note":"Distributes SV BioFlo for Spectrum Vascular.","iss":[0]}],"issues":[{"d":"Jul 2026","co":"Kimal PLC","p":"BioFlo PICC lines (6 codes)","s":"Temporary discontinuation while a new manufacturer awaits certification; delisted Jun/Jul 2026, resolution expected 01/12/2026.","use":"Trusts using BioFlo PICCs need an alternative until at least December 2026 — a live opening for other PICC suppliers.","url":"https://www.supplychain.nhs.uk/icn/product-update-kimal-plc-bioflo-picc-lines/"},{"d":"Apr 2026","co":"Becton Dickinson","p":"Venflon IV cannula with wings, 14G–22G","s":"Discontinued through rationalisation; progressive delisting since Jan 2025, all codes delisted by 31/07/2026.","use":"Any trust still on Venflon must convert now — an immediate switching conversation for every cannula supplier.","url":"https://www.supplychain.nhs.uk/icn/delisting-becton-dickinson-venflon-iv-cannula-with-wings-14g-to-22g/"},{"d":"May 2025","co":"Bard Access Systems / BD","p":"4Fr single-lumen PowerPICC (SOLO and non-SOLO)","s":"Global product removal for material-fatigue leak risk (FSN updated 15/05/2025); unused stock destroyed.","use":"Context for PICC conversations — ask what the trust moved to and whether the replacement is bedding in well.","url":"https://www.gov.uk/drug-device-alerts/field-safety-notices-19-to-23-may-2025"}]},"continence":{"label":"Continence & urology","route":[{"name":"NHS Supply Chain — Urology, Bowel and Faecal Management framework (18 lots, 57 suppliers)","dates":"21/02/2024 – 20/02/2027 (+12-month option)","url":"https://www.supplychain.nhs.uk/product-information/contract-launch-brief/urology-and-bowel-management/"}],"routeNote":"Acute route above (Lot 1 internal catheters, Lot 2 sheaths, Lot 3 drainage bags). In the community, most continence products are prescribed via Part IX of the Drug Tariff — a separate route with its own listing process.","types":{"ind":"Indwelling (Foley) catheters","ic":"Intermittent catheters","sh":"Sheaths","dr":"Drainage bags & valves"},"suppliers":[{"co":"Coloplast","brands":"SpeediCath · Conveen · Folysil","t":["ic","sh","dr","ind"],"url":"https://products.coloplast.co.uk/coloplast/continence-care/speedicath/","note":"Intermittent, sheath, drainage and indwelling ranges on the urology framework.","iss":[]},{"co":"Convatec","brands":"GentleCath · Cure","t":["ic"],"url":"https://www.convatec.com/en-gb/continence-care/","note":"GentleCath FeelClean technology; me+ user support programme.","iss":[]},{"co":"Hollister","brands":"VaPro · Infyna Chic","t":["ic"],"url":"https://www.hollister.co.uk/en-gb/continencecare","note":"Hydrophilic ranges.","iss":[]},{"co":"Wellspect","brands":"LoFric","t":["ic"],"url":"https://www.wellspect.co.uk/","note":"LoFric intermittent range.","iss":[]},{"co":"BD / Bard","brands":"Biocath · Bardex I.C.","t":["ind"],"url":"https://www.bd.com/en-uk/products-and-solutions/products/product-families/biocath-hydrogel-coated-latex-foley-catheter","note":"Bardex I.C. silver-alloy anti-infective Foley range.","iss":[]},{"co":"Teleflex Medical UK","brands":"Rüsch","t":["ind","ic"],"url":"https://www.teleflex.com/usa/en/product-areas/urology/","note":"On the urology framework. See live issues — the PTFE-coated latex Foley range is discontinued and being delisted.","iss":[0]},{"co":"Clinisupplies","brands":"Prosys · Liberty","t":["ic","ind","sh","dr"],"url":"https://www.clinisupplies.co.uk/category/bladder/","note":"Prosys/Great Bear accessories rebranded Liberty from 01/03.","iss":[]},{"co":"CliniMed","brands":"CliniSure · CliniFix","t":["ic","sh","dr"],"url":"https://www.clinimed.co.uk/continence-care/","note":"CliniFix is catheter securement.","iss":[]},{"co":"Optimum Medical","brands":"Ugo · OptiLube · ReadyCath","t":["ind","ic","sh","dr"],"url":"https://optimummedical.co.uk/","note":"Ugo leg bags, sheaths, catheter valve and Foley kits.","iss":[]}],"issues":[{"d":"Jul 2026","co":"Teleflex Medical UK","p":"Self-Retaining (Foley) 2-Way PTFE Coated Latex catheters, 12Ch to 22Ch (12 product codes)","s":"NHS Supply Chain ICN 3267, opened 24/02/2026 and last updated 09/07/2026, now marked Resolved and closed. Teleflex Medical UK has discontinued the range and NHS Supply Chain has requested all 12 codes be delisted from the catalogue. FSS137 is delisted and unavailable to order. Six codes remain available while remaining stocks last and will then be delisted: FSS134, FSS136, FSS138, FSS141, FSS142 and FSS143. Five were delisted at the 29/05/2026 update: FSS10089, FSS10090, FSS1028, FSS1029 and FSS144. Indirect alternatives are mapped on the online catalogue and a product matrix is available; NHS Supply Chain advises consulting clinical experts on suitability.","use":"Every trust still ordering these PTFE-coated latex Foley codes has to convert as stock runs out, so this is a live switching conversation for any indwelling-catheter supplier. Ask which of the six remaining codes they still order and whether an alternative has actually been agreed with their clinical team, or only mapped.","url":"https://www.supplychain.nhs.uk/icn/product-update-teleflex-medical-uk-self-retaining-catheter-foley-2-way-ptfe-coated-latex-sizes-12ch-to-22ch/"}]}};
var DATA_URL='https://raw.githubusercontent.com/louisehoult-beep/msh-compare-data/main/data/compare-issues.json';
/* Standing clusters: a group of notices that are really one story, pinned above
   the speciality picker so it is seen whatever the rep has selected. The
   chlorhexidine/ChloraPrep thread is the first — eight notices across two
   specialities, owning the two nearest deadlines in the whole feed. A cluster
   never restates a notice: it points at items that are already in the feed, so
   there is nothing here to fall out of step with the sources. */
var CLUSTERS=[];
var BYURL={};
try{fetch(DATA_URL,{cache:'no-store'}).then(function(r){return r.json();}).then(function(j){
  if(j){if(j.specialities){
    /* Merge the live feed. Until 30/07/2026 this line was `if(D[k])`, so any
       speciality without a hand-researched entry here was silently dropped:
       14 of 24 items were in the data file and invisible on this page, including
       every skin-prep notice. A speciality we have notices for is now shown even
       when the supplier comparison for it has not been built yet — the notices
       are the perishable, useful half, and dropping them was the bigger loss.
       Nothing is invented: a speciality with no researched suppliers says so. */
    for(var k in j.specialities){
      var f=j.specialities[k]; if(!f){continue;}
      if(D[k]){ if(f.issues){D[k].issues=f.issues;} if(f.label){D[k].label=f.label;} }
      else if(f.issues&&f.issues.length){
        D[k]={label:f.label||k,route:[],routeNote:'',types:{},suppliers:[],
              issues:f.issues,feedOnly:true};
      }
    }
    BYURL={};
    for(var sk in D){(D[sk].issues||[]).forEach(function(it){BYURL[(it.url||'').replace(/\/+$/,'')]=it;});}
    CLUSTERS=(j.clusters||[]).filter(function(c){return c&&c.urls&&c.urls.length;});
    var st=document.getElementById('cp-stamp');
    if(st){st.textContent='Frameworks and supply notices change \u2014 always open the source link and confirm current status before using anything in a conversation or tender.';}
    /* The feed can introduce specialities that were not in the dropdown when the
       pane was built, so the list is rebuilt before re-rendering. */
    if(document.getElementById('sec-comp')){renderClusters();fillSpecs();render();}
  }}
}).catch(function(e){});}catch(e){}
function esc(s){var d=document.createElement('div');d.textContent=s;return d.innerHTML;}
/* --- dates -----------------------------------------------------------------
   Until 05/08/2026 every return deadline and resolution date on this tab lived
   only inside the notice's summary text. A rep scanning the page could not see
   that a return deadline was two days away — they had to read eight paragraphs
   to find eight dates. Dates are now a field on the item (it.dates), so they can
   be shown, coloured and counted down.

   The countdown is DERIVED, so two rules apply to it. The source date is always
   printed beside it, never replaced by it, so a reader can check the arithmetic
   against the notice. And a date that has passed says so plainly rather than
   quietly disappearing or counting backwards — an expired deadline is a thing a
   rep needs to know they have missed. */
function dayGap(iso){
  var p=/^(\d{4})-(\d{2})-(\d{2})$/.exec(iso||'');
  if(!p){return null;}
  var then=Date.UTC(+p[1],+p[2]-1,+p[3]);
  var n=new Date(),now=Date.UTC(n.getFullYear(),n.getMonth(),n.getDate());
  return Math.round((then-now)/86400000);
}
function ukDate(iso){
  var p=/^(\d{4})-(\d{2})-(\d{2})$/.exec(iso||'');
  return p?(p[3]+'/'+p[2]+'/'+p[1]):String(iso||'');
}
function countdown(g){
  if(g===null){return '';}
  if(g<0){return g===-1?'yesterday':(-g)+' days ago';}
  if(g===0){return 'today';}
  if(g===1){return 'tomorrow';}
  if(g<=45){return 'in '+g+' days';}
  return '';
}
/* Colour is a function of how close a DEADLINE is, not of how alarming the
   notice sounds. A resolution date is never red: it is when a supplier hopes to
   be back, not something the trust must do. */
function dateTone(kind,g){
  if(kind==='resolved'){return ['#eef1f4','#d5dbe2','#5b6675'];}
  if(kind==='deadline'){
    if(g===null||g<0){return ['#f2f2f2','#dcdcdc','#5b6675'];}
    if(g<=7){return ['#fdecea','#f3bdb6','#b3261e'];}
    if(g<=30){return ['#fbf3df','#e8d5a8','#7a5b14'];}
    return ['#fff','#e3e7ec','#1c2430'];
  }
  return ['#fbf3df','#e8d5a8','#7a5b14'];
}
function datePills(it,big){
  var ds=(it&&it.dates)||[];
  if(!ds.length){return '';}
  var out='<div style="display:flex;flex-wrap:wrap;gap:6px;margin:0 0 8px;">';
  ds.forEach(function(dt){
    var g=dayGap(dt.on),c=dateTone(dt.kind,g),cd=countdown(g);
    var past=(dt.kind==='deadline'&&g!==null&&g<0);
    out+='<span style="display:inline-block;background:'+c[0]+';border:1px solid '+c[1]+';color:'+c[2]+
         ';border-radius:8px;padding:'+(big?'5px 10px':'3px 8px')+';font-size:'+(big?'12px':'11.5px')+';line-height:1.35;">';
    out+='<b>'+esc(dt.what||(dt.kind==='deadline'?'Deadline':'Anticipated resolution'))+'</b> '+esc(ukDate(dt.on));
    if(cd){out+=' &middot; <b>'+esc(cd)+'</b>';}
    if(past){out+=' &middot; <b>passed</b>';}
    if(dt.note){out+='<br><span style="opacity:.85;">'+esc(dt.note)+'</span>';}
    out+='</span>';
  });
  return out+'</div>';
}
/* --- standing clusters ------------------------------------------------------
   Pinned above the speciality picker, because the thing it covers does not
   belong to one speciality and a rep who picks "Cardiology" should still see
   that a chlorhexidine return deadline is two days out. Every row points at an
   item already in the feed, matched by URL, so the cluster cannot drift from
   the notices: if an item is removed, its row disappears with it. */
function renderClusters(){
  var box=document.getElementById('cp-clusters');
  if(!box){return;}
  if(!CLUSTERS.length){box.innerHTML='';return;}
  var h='';
  CLUSTERS.forEach(function(c){
    var rows=(c.urls||[]).map(function(u){return BYURL[(u||'').replace(/\/+$/,'')];}).filter(function(x){return x;});
    if(!rows.length){return;}
    /* Nearest live date first; anything dateless sits after the dated ones. */
    rows.sort(function(a,b){
      function key(it){
        var best=null;
        ((it.dates)||[]).forEach(function(d){var g=dayGap(d.on);if(g===null){return;}
          var w=(d.kind==='deadline'?0:1)*100000+(g<0?90000+(-g):g);
          if(best===null||w<best){best=w;}});
        return best===null?1e9:best;
      }
      return key(a)-key(b);
    });
    h+='<div style="background:#fff;border:1px solid #e3e7ec;border-left:4px solid #b3261e;border-radius:0 14px 14px 0;padding:14px 16px;margin:0 0 16px;">';
    h+='<div style="font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:#b3261e;font-weight:700;margin-bottom:6px;">Standing thread &mdash; pinned, whatever speciality you pick</div>';
    h+='<h4 style="font-size:15px;margin:0 0 6px;">'+esc(c.title||'')+'</h4>';
    if(c.blurb){h+='<p style="font-size:13px;color:#3d4757;line-height:1.55;margin:0 0 8px;">'+esc(c.blurb)+'</p>';}
    h+='<div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:10px;margin:10px 0 0;">';
    rows.forEach(function(it){
      h+='<div style="border:1px solid #eef1f4;border-radius:10px;padding:10px 12px;">';
      h+='<div style="font-size:11px;font-weight:700;color:#b3261e;">'+esc(it.d||'')+(it.co?' &middot; '+esc(it.co):'')+'</div>';
      h+='<div style="font-size:12.5px;font-weight:700;margin:3px 0 6px;">'+esc(it.p||'')+'</div>';
      h+=datePills(it);
      h+='<a href="'+it.url+'" target="_blank" rel="noopener" style="font-size:11.5px;color:#a37519;font-weight:600;">source &rarr;</a></div>';
    });
    h+='</div>';
    if(c.rule){h+='<p style="font-size:11.5px;color:#75808d;line-height:1.5;margin:10px 0 0;">'+esc(c.rule)+'</p>';}
    h+='</div>';
  });
  box.innerHTML=h;
}
function el(tag,attrs,html){var e=document.createElement(tag);if(attrs){for(var k in attrs){e.setAttribute(k,attrs[k]);}}if(html!=null){e.innerHTML=html;}return e;}
function buildPane(){
  var sec=el('section',{id:'sec-comp'});
  var h='';
  h+='<div class="mst__calcintro">';
  h+='<h3 class="mst__ih">What this does</h3>';
  h+='<p class="mst__ip">Shows the competitor set on the live NHS Supply Chain framework for your product type &mdash; with current recalls, delistings and supply gaps. Every fact comes from reputable, verified NHS sources, linked throughout.</p>';
  h+='<p class="mst__ip" id="cp-stamp" style="color:#75808d;">Frameworks and supply notices change &mdash; always open the source link and confirm current status before using anything in a conversation or tender.</p>';
  h+='</div>';
  h+='<div id="cp-clusters"></div>';
  h+='<div class="mst__controls">';
  h+='<label class="mst__lab">1 &middot; Your company (optional)<select class="mst__sel" id="cp-me"></select></label>';
  h+='<label class="mst__lab">2 &middot; Speciality<select class="mst__sel" id="cp-spec"></select></label>';
  h+='<label class="mst__lab">3 &middot; Product type<select class="mst__sel" id="cp-type"></select></label>';
  h+='</div>';
  h+='<div id="cp-route"></div><div id="cp-table"></div><div id="cp-issues"></div>';
  h+='<div class="mst__note">Supplier sets are the key players for each product type, not the full framework award list (the continence framework alone has 57 suppliers). Brands, framework presence and issues verified against the linked sources on 17/07/2026. For anything time-critical, check the NHS Supply Chain <a href="https://www.supplychain.nhs.uk/icn/" target="_blank" rel="noopener">important customer notices</a> and <a href="https://www.gov.uk/drug-device-alerts" target="_blank" rel="noopener">MHRA alerts</a> directly.</div>';
  sec.innerHTML=h;
  return sec;
}
function fill(sel,opts){sel.innerHTML='';opts.forEach(function(o){var op=document.createElement('option');op.value=o[0];op.textContent=o[1];sel.appendChild(op);});}
/* The speciality list is built from the data, not hard-coded. Ones with a
   researched supplier set come first, because those give the full comparison;
   the rest follow, labelled with how many live notices they carry so nobody
   picks one expecting a supplier table. onlyCo (optional) restricts the list
   to specialities where that company is a listed supplier — the step-1-company
   filter feeding into step 2. */
function fillSpecs(onlyCo){
  var sel=document.getElementById('cp-spec');
  if(!sel){return;}
  var full=[],feed=[];
  for(var k in D){
    var S=D[k],n=(S.issues||[]).length;
    if(onlyCo&&!(S.suppliers||[]).some(function(s){return s.co===onlyCo;})){continue;}
    if(S.suppliers&&S.suppliers.length){full.push([k,S.label]);}
    else if(!onlyCo&&n){feed.push([k,S.label+' — '+n+' live notice'+(n===1?'':'s')+', no supplier set yet']);}
  }
  full.sort(function(a,b){return a[1].localeCompare(b[1]);});
  feed.sort(function(a,b){return a[1].localeCompare(b[1]);});
  var prev=sel.value;
  fill(sel,[['','Select…']].concat(full,feed));
  if(prev&&Array.prototype.some.call(sel.options,function(o){return o.value===prev;})){sel.value=prev;}
}
/* Step 1: every company across every speciality, deduped — populated once at
   mount, independent of whatever speciality is later chosen. */
function allCompanies(){
  var seen={},out=[];
  for(var k in D){
    (D[k].suppliers||[]).forEach(function(s){if(!seen[s.co]){seen[s.co]=1;out.push(s.co);}});
  }
  out.sort(function(a,b){return a.localeCompare(b);});
  return out;
}
function fillCompanies(){
  var sel=document.getElementById('cp-me');
  if(!sel){return;}
  var prev=sel.value;
  fill(sel,[['','— all companies —']].concat(allCompanies().map(function(c){return [c,c];})));
  if(prev){sel.value=prev;}
}
/* Step 1 -> 2: picking a company narrows Speciality to the ones that company
   actually supplies into; clearing it restores the full list. */
function onCompany(){
  var co=document.getElementById('cp-me').value;
  fillSpecs(co||null);
  onSpec();
}
function render(){
  var spec=document.getElementById('cp-spec').value;
  if(!spec){return;}
  var S=D[spec];
  var type=document.getElementById('cp-type').value;
  var me=document.getElementById('cp-me').value;
  /* A speciality we have notices for but no researched framework or supplier set.
     It says exactly that. No supplier table is drawn, because inventing one to
     fill the space is how a comparison tool starts lying to a rep in a meeting. */
  if(S.feedOnly){
    var fo='<div style="background:#fff;border:1px solid #e3e7ec;border-left:3px solid #a37519;border-radius:0 10px 10px 0;padding:12px 15px;margin:0 0 14px;">';
    fo+='<div style="font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:#a37519;font-weight:700;margin-bottom:6px;">Live notices only &mdash; no supplier comparison yet</div>';
    fo+='<p style="font-size:13px;margin:0 0 5px;">We are tracking recalls, delistings and supply gaps in <b>'+esc(S.label)+'</b>, but the framework and competitor set for it have not been researched yet, so there is no comparison table below &mdash; only the notices themselves.</p>';
    /* This sentence used to name the two researched specialities by hand. The
       moment a third is built out it would have been a lie on a paying page, and
       the whole direction of travel is more specialities, not fewer — so it is
       read off the data instead. */
    var built=[];
    for(var bk in D){if((D[bk].suppliers||[]).length){built.push(D[bk].label||bk);}}
    built.sort(function(a,b){return a.localeCompare(b);});
    var WORD=['no','one','two','three','four','five','six','seven','eight','nine','ten'];
    var builtTxt=built.length?(built.length===1?built[0]+' is the one speciality with':
      built.slice(0,-1).join(', ')+' and '+built[built.length-1]+' are the '+
      (WORD[built.length]||built.length)+' with'):'No speciality yet has';
    fo+='<p style="font-size:12px;color:#5b6675;margin:4px 0 0;">'+esc(builtTxt)+' a full supplier comparison. If this speciality is one you sell into, say so and it can be built out.</p></div>';
    document.getElementById('cp-route').innerHTML=fo;
    document.getElementById('cp-table').innerHTML='';
    renderIssues(S);
    return;
  }
  var route='<div style="background:#fff;border:1px solid #e3e7ec;border-left:3px solid #a37519;border-radius:0 10px 10px 0;padding:12px 15px;margin:0 0 14px;">';
  route+='<div style="font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:#a37519;font-weight:700;margin-bottom:6px;">Route to market &mdash; live framework</div>';
  S.route.forEach(function(r){route+='<p style="font-size:13px;margin:0 0 5px;"><b>'+esc(r.name)+'</b> &middot; '+esc(r.dates)+' &middot; <a href="'+r.url+'" target="_blank" rel="noopener" style="color:#a37519;">framework brief &rarr;</a></p>';});
  route+='<p style="font-size:12px;color:#5b6675;margin:4px 0 0;">'+esc(S.routeNote)+'</p></div>';
  document.getElementById('cp-route').innerHTML=route;
  var rows='';
  var shown=0;
  S.suppliers.forEach(function(s){
    if(type!=='all'){if(s.t.indexOf(type)<0){return;}}
    shown=shown+1;
    var mine=(s.co===me);
    var flags='';
    s.iss.forEach(function(i){var it=S.issues[i];if(it){flags+='<a href="#cp-iss-'+i+'" style="display:inline-block;background:#fbf3df;border:1px solid #e8d5a8;color:#7a5b14;font-size:10.5px;font-weight:700;border-radius:99px;padding:1px 8px;margin:2px 4px 0 0;text-decoration:none;">&#9888; '+esc(it.d)+'</a>';}});
    rows+='<tr style="'+(mine?'background:#f3ead2;':'')+'">';
    rows+='<td style="padding:10px 14px;border-bottom:1px solid #f0ece3;vertical-align:top;"><b>'+esc(s.co)+'</b>'+(mine?' <span style="font-size:10px;font-weight:700;color:#a37519;">(YOU)</span>':'')+'</td>';
    rows+='<td style="padding:10px 14px;border-bottom:1px solid #f0ece3;vertical-align:top;">'+esc(s.brands)+'</td>';
    rows+='<td style="padding:10px 14px;border-bottom:1px solid #f0ece3;vertical-align:top;font-size:12px;color:#5b6675;">'+s.t.map(function(k){return esc(S.types[k]||k);}).join(', ')+'</td>';
    rows+='<td style="padding:10px 14px;border-bottom:1px solid #f0ece3;vertical-align:top;font-size:12px;color:#5b6675;">'+esc(s.note)+' '+flags+'</td>';
    rows+='<td style="padding:10px 14px;border-bottom:1px solid #f0ece3;vertical-align:top;"><a href="'+s.url+'" target="_blank" rel="noopener" style="color:#a37519;font-weight:600;font-size:12px;">site &rarr;</a></td>';
    rows+='</tr>';
  });
  var tbl='<div style="background:#fff;border:1px solid #e3e7ec;border-radius:14px;overflow:auto;margin:0 0 14px;">';
  tbl+='<table style="width:100%;border-collapse:collapse;font-size:13px;min-width:640px;"><thead><tr>';
  ['Company','Key brands','Covers','Framework notes &amp; flags','Link'].forEach(function(c){tbl+='<th style="text-align:left;font-size:10.5px;letter-spacing:.08em;text-transform:uppercase;color:#5b6675;padding:10px 14px;border-bottom:1px solid #e3e7ec;">'+c+'</th>';});
  tbl+='</tr></thead><tbody>'+rows+'</tbody></table></div>';
  if(shown===0){tbl='<p style="font-size:13px;color:#5b6675;">No verified suppliers recorded for this product type yet.</p>';}
  document.getElementById('cp-table').innerHTML=tbl;
  renderIssues(S);
}
function renderIssues(S){
  var iss='<div style="font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:#b3261e;font-weight:700;margin:0 0 8px;">Live issues you can use &mdash; recalls, delistings, supply gaps</div>';
  if(!(S.issues||[]).length){
    document.getElementById('cp-issues').innerHTML=iss+'<p style="font-size:13px;color:#5b6675;">No open recalls, delistings or supply gaps recorded for this speciality right now. That is a real answer, not a gap in the data.</p>';
    return;
  }
  iss+='<div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:12px;">';
  S.issues.forEach(function(it,i){
    iss+='<div id="cp-iss-'+i+'" style="background:#fff;border:1px solid #e3e7ec;border-top:3px solid #b3261e;border-radius:12px;padding:13px 15px;">';
    iss+='<div style="font-size:11px;font-weight:700;color:#b3261e;">'+esc(it.d)+(it.co?' &middot; '+esc(it.co):'')+'</div>';
    iss+='<div style="font-size:13px;font-weight:700;margin:3px 0 6px;">'+esc(it.p)+'</div>';
    iss+=datePills(it,true);
    iss+='<p style="font-size:12.5px;color:#5b6675;line-height:1.55;margin:0 0 6px;">'+esc(it.s)+'</p>';
    if(it.use){iss+='<p style="font-size:12.5px;color:#1c2430;line-height:1.55;margin:0 0 6px;"><b>How to use it:</b> '+esc(it.use)+'</p>';}
    if(it.autoDetected){iss+='<p style="font-size:11px;color:#b3261e;font-weight:700;letter-spacing:.04em;margin:0 0 6px;">NEW \u2014 auto-detected, verify at source</p>';}
    /* Why an item sits where it does, and what could not be checked, both belong
       on the card. A reclassified notice looks like a mistake to anyone who saw
       it filed elsewhere yesterday, and a gated source looks like thin research
       unless it says it is gated. */
    if(it.reclassified){iss+='<p style="font-size:11.5px;color:#75808d;line-height:1.5;margin:0 0 6px;"><b>Filed here because:</b> '+esc(it.reclassified)+'</p>';}
    if(it.gated){iss+='<p style="font-size:11.5px;color:#75808d;line-height:1.5;margin:0 0 6px;"><b>Not fully verifiable:</b> '+esc(it.gated)+'</p>';}
    iss+='<a href="'+it.url+'" target="_blank" rel="noopener" style="font-size:11.5px;color:#a37519;font-weight:600;">source &rarr;</a></div>';
  });
  iss+='</div>';
  document.getElementById('cp-issues').innerHTML=iss;
}
function onSpec(){
  var spec=document.getElementById('cp-spec').value;
  if(!spec){document.getElementById('cp-route').innerHTML='';document.getElementById('cp-table').innerHTML='';document.getElementById('cp-issues').innerHTML='';fill(document.getElementById('cp-type'),[['all','All product types']]);return;}
  var S=D[spec];
  var t=[['all','All product types']];
  for(var k in S.types){t.push([k,S.types[k]]);}
  fill(document.getElementById('cp-type'),t);
  render();
}
/* Mounts directly into its own container on the "Know the field & compare"
   page (#msh-compare-speciality) — it no longer steals a tab out of the
   Stakeholder Mapper box. Two different things were both called "compare"
   in two different places on the page; this is now the one home for it. */
function mount(){
  var MOUNT=document.getElementById('msh-compare-speciality');
  if(!MOUNT){return false;}
  if(document.getElementById('cp-spec')){return true;}
  var pane=buildPane();
  MOUNT.appendChild(pane);
  fillCompanies();
  renderClusters();
  fillSpecs();
  onSpec();
  document.getElementById('cp-me').addEventListener('change',onCompany);
  document.getElementById('cp-spec').addEventListener('change',onSpec);
  document.getElementById('cp-type').addEventListener('change',render);
  return true;
}
if(document.readyState!=='loading'){mount();}else{document.addEventListener('DOMContentLoaded',mount);}
var n=0;
var iv=setInterval(function(){ if(mount()){clearInterval(iv);return;} n=n+1; if(n>25){clearInterval(iv);} },400);
})();